name: windows build

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - uses: snickerbockers/submodules-init@v3
    - uses: snickerbockers/cmake-configure@9d04b639e7b88d397d4af49ed6e1b09e64edb2a7
      with:
        generator: 'MinGW Makefiles'
